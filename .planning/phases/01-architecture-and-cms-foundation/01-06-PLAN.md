---
phase: 01-architecture-and-cms-foundation
plan: 06
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/blocks/property-card-block.tsx
  - src/components/blocks/trust-block.tsx
  - src/components/blocks/how-it-works-block.tsx
  - src/components/blocks/pain-point-block.tsx
  - src/components/blocks/benefits-block.tsx
autonomous: true
requirements:
  - BLOCK-06
  - BLOCK-07
  - BLOCK-08
  - BLOCK-09
  - BLOCK-10

must_haves:
  truths:
    - "PropertyCardBlock renders listing previews with price, bedrooms, bathrooms, and address"
    - "TrustBlock renders testimonials and/or stats for social proof"
    - "HowItWorksBlock renders numbered steps in a process layout"
    - "PainPointBlock renders problem/solution pairs"
    - "BenefitsBlock renders feature/benefit cards in a grid"
  artifacts:
    - path: "src/components/blocks/property-card-block.tsx"
      provides: "Property listing card grid"
      min_lines: 25
    - path: "src/components/blocks/trust-block.tsx"
      provides: "Social proof with testimonials and stats"
      min_lines: 30
    - path: "src/components/blocks/how-it-works-block.tsx"
      provides: "Numbered steps process"
      min_lines: 20
    - path: "src/components/blocks/pain-point-block.tsx"
      provides: "Problem/solution layout"
      min_lines: 20
    - path: "src/components/blocks/benefits-block.tsx"
      provides: "Feature/benefit card grid"
      min_lines: 20
  key_links:
    - from: "src/components/blocks/property-card-block.tsx"
      to: "next/link"
      via: "Link to property detail pages"
      pattern: "import.*Link.*next/link"
    - from: "all 5 blocks"
      to: "src/types/blocks.ts"
      via: "prop type imports"
      pattern: "import.*from.*@/types/blocks"
---

<objective>
Build the remaining 5 of 10 reusable block components: PropertyCardBlock, TrustBlock, HowItWorksBlock, PainPointBlock, and BenefitsBlock. These complete the composable block system that all page templates in Phase 2 will use.

Purpose: Completes the 10-block component library. PropertyCardBlock is essential for tenant-facing pages. TrustBlock provides social proof on landing pages. HowItWorks, PainPoint, and Benefits blocks are used in CityService pages (the highest-value SEO pages).
Output: 5 block components, completing the full set of 10.
</objective>

<execution_context>
@/Users/sakshamsolanki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sakshamsolanki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md

<!-- Depends on Plan 01 for shadcn/ui components (Card, Button) -->
<!-- Uses prop types from Plan 05 (src/types/blocks.ts) -- runs in same wave, but blocks.ts should exist from Plan 05 Task 1 -->
@.planning/phases/01-architecture-and-cms-foundation/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Block prop types from src/types/blocks.ts (created in Plan 05) -->
PropertyCardBlockProps: { listings: PropertyCardData[] }
TrustBlockProps: { testimonials?: TestimonialData[]; stats?: Array<{ label: string; value: string }>; variant?: 'testimonials' | 'stats' | 'combined' }
HowItWorksBlockProps: { steps: HowItWorksStep[]; title?: string }
PainPointBlockProps: { painPoints: PainPointData[]; title?: string }
BenefitsBlockProps: { benefits: BenefitData[]; title?: string; columns?: 2 | 3 | 4 }
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: PropertyCardBlock and TrustBlock</name>
  <files>src/components/blocks/property-card-block.tsx, src/components/blocks/trust-block.tsx</files>
  <action>
1. Create `src/components/blocks/property-card-block.tsx` as a React Server Component:
   - Import `PropertyCardBlockProps` from `@/types/blocks`
   - Import `Card`, `CardContent`, `CardHeader`, `CardTitle` from `@/components/ui/card`
   - Import `Link` from `next/link`
   - Import `Image` from `next/image`
   - Import `Bed`, `Bath`, `Maximize`, `MapPin` icons from `lucide-react`
   - Render a responsive grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`
   - Each listing renders as a Card wrapped in `Link` to `/ca/{provinceSlug}/{citySlug}/rentals/{slug}/` (or similar -- exact URL pattern TBD in Phase 2, use a placeholder pattern for now)
   - Card contents:
     - Image at top (if imageUrl, use next/image with aspect-video, alt from imageAlt)
     - Price displayed prominently: formatted as "$X,XXX/mo"
     - Icon row: Bed icon + bedroom count, Bath icon + bathroom count, Maximize icon + sqft (if available)
     - Address with MapPin icon
     - Availability badge: green "Available" or grey "Not Available"
   - Hover effect on cards (shadow elevation)

2. Create `src/components/blocks/trust-block.tsx` as a React Server Component:
   - Import `TrustBlockProps` from `@/types/blocks`
   - Import `Card`, `CardContent` from `@/components/ui/card`
   - Import `Quote` icon from `lucide-react`
   - Render based on `variant` prop:

   **variant="testimonials"** (default if testimonials provided):
   - Grid of testimonial cards: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`
   - Each card: quote icon, quote text in italics, name in bold, city below name, outcome text if provided (styled as a highlighted stat)

   **variant="stats"**:
   - Horizontal row of stat cards: `flex flex-wrap justify-center gap-8`
   - Each stat: large value text (text-4xl font-bold), label below (text-sm text-muted-foreground)
   - Example stats: "2,500+ Properties Managed", "98% Tenant Placement Rate", "20+ Cities"

   **variant="combined"**:
   - Stats row at top, testimonials grid below

   - Section has a subtle background (bg-gray-50) to visually distinguish from adjacent blocks
   - Section title: "What Our Clients Say" or "Trusted by Property Owners" (default, overridable)
  </action>
  <verify>
    <automated>cd "/Users/sakshamsolanki/Desktop/02-Client-Work/Canadian real estate" && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>PropertyCardBlock renders listing cards with price, bedrooms, bathrooms, sqft, address, and availability. TrustBlock renders testimonials, stats, or combined variant. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: HowItWorksBlock, PainPointBlock, and BenefitsBlock</name>
  <files>src/components/blocks/how-it-works-block.tsx, src/components/blocks/pain-point-block.tsx, src/components/blocks/benefits-block.tsx</files>
  <action>
1. Create `src/components/blocks/how-it-works-block.tsx` as a React Server Component:
   - Import `HowItWorksBlockProps` from `@/types/blocks`
   - Render a section with title (default: "How It Works", overridable via `title` prop)
   - Display steps in a numbered process layout:
     - Desktop: horizontal timeline with step numbers in circles connected by lines
     - Mobile: vertical list with step numbers
   - Each step: Large step number in a circle (bg-primary text-primary-foreground rounded-full w-12 h-12 flex items-center justify-center), title below, description text below title
   - Steps connected by a horizontal line on desktop (`hidden md:block` border between circles)
   - Max 6 steps (display warning if more -- but no runtime error)

2. Create `src/components/blocks/pain-point-block.tsx` as a React Server Component:
   - Import `PainPointBlockProps` from `@/types/blocks`
   - Import `AlertCircle`, `CheckCircle` icons from `lucide-react`
   - Render a section with title (default: "Common Challenges & Our Solutions", overridable)
   - Layout: alternating problem/solution pairs
   - Each pair rendered as a two-column grid on desktop (1 col on mobile):
     - Left column (problem): AlertCircle icon (text-red-500) + problem text, subtle red-tinted background (bg-red-50)
     - Right column (solution): CheckCircle icon (text-green-500) + solution text, subtle green-tinted background (bg-green-50)
   - Alternating pairs can swap left/right for visual variety (even-indexed pairs reverse on desktop)

3. Create `src/components/blocks/benefits-block.tsx` as a React Server Component:
   - Import `BenefitsBlockProps` from `@/types/blocks`
   - Import `Card`, `CardContent`, `CardHeader`, `CardTitle` from `@/components/ui/card`
   - Import icons from `lucide-react` using same ICON_MAP pattern as ServiceGridBlock (or a shared icon map utility)
   - Render a section with title (default: "Benefits", overridable)
   - Responsive grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-{columns || 3} gap-6`
   - Each benefit as a Card:
     - Icon at top (from ICON_MAP, fallback to a default icon like `Star`)
     - Title (CardTitle)
     - Description text (CardContent)
   - Cards have hover effect (subtle shadow increase)
   - If icon prop is not provided or not in map, use a default icon
  </action>
  <verify>
    <automated>cd "/Users/sakshamsolanki/Desktop/02-Client-Work/Canadian real estate" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>HowItWorksBlock renders numbered steps with horizontal timeline on desktop. PainPointBlock renders alternating problem/solution pairs with color-coded backgrounds. BenefitsBlock renders benefit cards in a responsive grid with icons. All 10 block components now exist. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. PropertyCardBlock renders listings with price, beds, baths, sqft, address, availability
3. TrustBlock renders testimonials, stats, and combined variants
4. HowItWorksBlock renders numbered steps in timeline layout
5. PainPointBlock renders problem/solution pairs with color coding
6. BenefitsBlock renders benefit cards with icons in responsive grid
7. All 10 block component files now exist in src/components/blocks/
8. All blocks import typed props from src/types/blocks.ts
</verification>

<success_criteria>
- All 5 remaining block components render visually correct with typed props
- PropertyCardBlock displays all key listing details (price, beds, baths, sqft, address)
- TrustBlock supports 3 variants (testimonials, stats, combined)
- HowItWorksBlock shows numbered steps in process layout
- PainPointBlock shows problem/solution pairs with visual distinction
- BenefitsBlock shows feature cards with icons
- Complete set of 10 block components exists
- Build succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-architecture-and-cms-foundation/01-06-SUMMARY.md`
</output>
