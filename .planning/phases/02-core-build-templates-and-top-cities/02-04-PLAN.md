---
phase: 02-core-build-templates-and-top-cities
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/(site)/owners/page.tsx
  - src/app/(site)/services/page.tsx
  - src/app/(site)/services/[service]/page.tsx
  - src/app/(site)/locations/page.tsx
  - src/sanity/queries/service.ts
autonomous: true
requirements:
  - TMPL-02
  - TMPL-04
  - TMPL-05
  - TMPL-15
  - OWN-01
  - OWN-02
  - OWN-05
  - SCHEMA-04
  - SCHEMA-06

must_haves:
  truths:
    - "Owner hub page renders hero, pain points, benefits, service grid, trust section, FAQ, and CTA with owner-specific messaging"
    - "Services hub page lists all 8 services with descriptions and links"
    - "Each service detail page renders full content body from CMS with Service JSON-LD"
    - "Locations hub page shows cities grouped by province with CityGridBlock"
    - "BreadcrumbNav present on service detail and locations pages"
    - "Contract-required owner messaging (nothing upfront, self-serve, account manager, MLS, screening, rent protection) appears on owner hub"
  artifacts:
    - path: "src/app/(site)/owners/page.tsx"
      provides: "Owner hub template with full owner messaging"
      min_lines: 80
    - path: "src/app/(site)/services/[service]/page.tsx"
      provides: "Service detail template with CMS body + JSON-LD"
      min_lines: 60
    - path: "src/app/(site)/locations/page.tsx"
      provides: "Locations hub with province-grouped cities"
      min_lines: 40
  key_links:
    - from: "src/app/(site)/owners/page.tsx"
      to: "src/sanity/queries/service.ts"
      via: "sanityFetch for owner services"
      pattern: "SERVICE_LIST_QUERY"
    - from: "src/app/(site)/services/[service]/page.tsx"
      to: "src/sanity/queries/service.ts"
      via: "sanityFetch for service detail"
      pattern: "SERVICE_PAGE_QUERY"
    - from: "src/app/(site)/services/[service]/page.tsx"
      to: "src/lib/schema-builders/service-schema.ts"
      via: "buildServiceSchema"
      pattern: "buildServiceSchema"
---

<objective>
Build the Owners hub, Service detail template, and Locations hub -- the three pages that form the owner-facing acquisition funnel.

Purpose: Property owners are the primary revenue-generating audience. These pages must convince landlords to sign up. The owner hub surfaces all contract-required messaging points. Service detail pages provide deep content per service. The locations hub shows geographic coverage.

Output: 3 fully functional page templates with CMS data fetching, JSON-LD, and breadcrumb navigation.
</objective>

<execution_context>
@/Users/sakshamsolanki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sakshamsolanki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-build-templates-and-top-cities/02-RESEARCH.md
@.planning/phases/02-core-build-templates-and-top-cities/02-01-SUMMARY.md

<interfaces>
From src/sanity/queries/service.ts:
```typescript
export const SERVICE_PAGE_QUERY = groq`*[_type == "service" && slug.current == $slug][0] { ... }`
export const SERVICE_LIST_QUERY = groq`*[_type == "service"] { slug }`
```

From src/sanity/queries/province.ts (created in Plan 01):
```typescript
export const PROVINCE_CITIES_QUERY  // cities by province for CityGridBlock
export const PROVINCE_LIST_QUERY    // all provinces for grouping
```

From src/components/layout/breadcrumb-nav.tsx:
```typescript
interface BreadcrumbCrumb { label: string; href: string }
interface BreadcrumbNavProps { crumbs: BreadcrumbCrumb[] }
export function BreadcrumbNav({ crumbs }: BreadcrumbNavProps)
```

From src/components/portable-text.tsx (created in Plan 01):
```typescript
export function PortableTextBody({ value }: { value: PortableTextBlock[] })
```

From src/lib/schema-builders/index.ts:
```typescript
export { buildServiceSchema } from './service-schema'
```

Block component props: See Plan 03 interfaces (same block props).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Owners hub and Locations hub templates</name>
  <files>
    src/app/(site)/owners/page.tsx,
    src/app/(site)/locations/page.tsx
  </files>
  <action>
    **Owners hub (src/app/(site)/owners/page.tsx):**

    Replace skeleton with full owner hub template. This is a Server Component.

    generateMetadata: title "Property Owners | MoveSmart Rentals", description targeting landlord/investor search intent (OWN-01): "Professional property management for Ontario landlords and investors. Zero upfront cost, dedicated account manager, tenant screening, and rent protection."

    Data fetching: sanityFetch with a query for all services where audience is 'owner' or 'both', ordered by `order asc`. Tags: ['service'].

    Page sections (in order):
    1. HeroBlock -- headline: "Property Management That Pays for Itself", subheadline about zero upfront cost model, cta1: "Create Free Account" -> /contact/, cta2: "Submit Property" -> /contact/, priority: true
    2. PainPointBlock -- 4 owner pain points (vacant units, bad tenants, management overhead, hidden fees) with MoveSmart solutions
    3. BenefitsBlock -- OWN-05 contract messaging as benefits (title: "Why Owners Choose MoveSmart"):
       - "Nothing Upfront" / "Our success-based model means you pay nothing until your property is rented"
       - "Self-Serve Online" / "Manage your properties, view reports, and communicate through your online portal"
       - "Dedicated Account Manager" / "A single point of contact who knows your properties inside and out"
       - "MLS Distribution" / "Your listings appear on MLS, Realtor.ca, and 50+ rental sites for maximum exposure"
       - "Structured Screening" / "Credit checks, employment verification, references, and rental history for every applicant"
       - "Rent Protection" / "Our rent guarantee program protects your income from missed payments"
       - "Property Preparation" / "Professional cleaning, staging, and photography before listing"
       - "Portal Visibility" / "Track applications, maintenance, and rent payments in real time"
       - "Tech + Brick-and-Mortar" / "Local market expertise backed by modern technology"
    4. ServiceGridBlock -- filtered owner services from CMS query, columns=3
    5. TrustBlock -- stats variant with key numbers: "500+" properties managed, "98%" tenant satisfaction, "14 days" average placement time, "20+" cities served
    6. FAQBlock -- 4-5 owner-specific FAQs about pricing, services, tenant screening, rent guarantees
    7. CTABannerBlock -- headline: "Start Managing Smarter Today", primaryCta: "Create Free Account" -> /contact/, secondaryCta: "Book a Call" -> /contact/

    **Locations hub (src/app/(site)/locations/page.tsx):**

    Replace skeleton with locations hub. Server Component.

    generateMetadata: title "Locations | MoveSmart Rentals", description: "Professional property management across Ontario. Find MoveSmart Rentals services in your city."

    Data fetching: Fetch all provinces with their cities using sanityFetch. Use PROVINCE_LIST_QUERY to get all provinces, then for each province use PROVINCE_CITIES_QUERY, OR create a single efficient query. Tags: ['province', 'city'].

    Page sections:
    1. HeroBlock -- headline: "Find MoveSmart in Your City", subheadline: "Professional property management across Ontario and expanding nationwide"
    2. For each province (grouped): section with h2 province name, CityGridBlock with that province's cities
    3. CTABannerBlock -- "Do not see your city? We are expanding" with contact CTA

    BreadcrumbNav: crumbs [{ label: 'Home', href: '/' }, { label: 'Locations', href: '/locations/' }]
  </action>
  <verify>
    <automated>cd /tmp/movesmart-project 2>/dev/null || cd "/Users/sakshamsolanki/Desktop/02-Client-Work/Canadian real estate" && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Owners hub renders all OWN-05 messaging points plus service grid; Locations hub shows cities grouped by province; both compile cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Build Service detail template with CMS body and JSON-LD</name>
  <files>src/app/(site)/services/[service]/page.tsx</files>
  <action>
    Replace service detail skeleton with full template. Server Component.

    **generateStaticParams:** Use SERVICE_LIST_QUERY to pre-render all service pages.

    **generateMetadata:** Fetch service SEO fields, use generatePageMetadata with path `/services/${slug}`, fallbackTitle: `${service.title} | MoveSmart Rentals`, fallbackDescription: service.shortDescription.

    **Page component:**
    1. Fetch full service data with SERVICE_PAGE_QUERY using sanityFetch with tags ['service']
    2. If no data, call notFound() (SEO-08 pattern)
    3. Build JSON-LD: buildServiceSchema with service name, description (shortDescription), url, provider ("MoveSmart Rentals"), areaServed "Ontario, Canada"
    4. Inject via JsonLd component

    **Page sections:**
    1. BreadcrumbNav -- crumbs: Home -> Services -> {service.title}. Note: Services is not a page route itself, so the breadcrumb for "Services" should use href "/services/" even though there is no services/page.tsx (it will be a logical breadcrumb -- the services hub is the locations page or we just make it a non-link breadcrumb). Actually, the services hub IS a planned page type (TMPL-04 Services hub). We need to create a services index page. For now, link breadcrumb to "#" or create a simple services index.

    Actually, looking at the route structure, there is `services/[service]/page.tsx` but no `services/page.tsx` (services hub). The Services hub (TMPL-04) should be at `/services/`. Create a simple services index at `src/app/(site)/services/page.tsx` that lists all services.

    Create TWO files in this task:
    - `src/app/(site)/services/page.tsx` (Services hub -- TMPL-04)
    - `src/app/(site)/services/[service]/page.tsx` (Service detail -- TMPL-15)

    **Services hub (src/app/(site)/services/page.tsx):**
    - generateMetadata: title "Our Services | MoveSmart Rentals", description about 8 property management services
    - Fetch all services ordered by `order asc`
    - BreadcrumbNav: Home -> Services
    - HeroBlock: "Our Property Management Services", subheadline about comprehensive services
    - ServiceGridBlock with all services, columns=3
    - CTABannerBlock

    **Service detail (src/app/(site)/services/[service]/page.tsx):**
    1. BreadcrumbNav: Home -> Services -> {title}
    2. HeroBlock with service hero image, title as headline, shortDescription as subheadline, CTA to /contact/
    3. PortableTextBody rendering service.body (if exists)
    4. FAQBlock with service.faq (if exists)
    5. CityGridBlock showing cities where this service is available (query cities that have CityService docs referencing this service)
    6. CTABannerBlock with "Get {service.title} Today" headline

    For the CityGridBlock of available cities, add a GROQ sub-query within SERVICE_PAGE_QUERY or create a separate query: `*[_type == "cityService" && service._ref == ^._id] { "title": city->title, "slug": city->slug.current, "provinceSlug": city->province->slug.current, "population": city->population, "medianRent": city->medianRent }`. If modifying the existing query is needed, update src/sanity/queries/service.ts to include this.
  </action>
  <verify>
    <automated>cd /tmp/movesmart-project 2>/dev/null || cd "/Users/sakshamsolanki/Desktop/02-Client-Work/Canadian real estate" && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Services hub lists all services; service detail renders CMS body, FAQ, available cities, and Service JSON-LD; breadcrumbs present; notFound() guard for missing services</done>
</task>

</tasks>

<verification>
- Owner hub has all 9 OWN-05 messaging points visible
- Service detail pages have Service JSON-LD in source
- Locations hub shows cities grouped by province
- BreadcrumbNav with BreadcrumbList JSON-LD present on service and locations pages
- All pages compile with TypeScript
</verification>

<success_criteria>
- Owners hub fully implements OWN-01, OWN-05 messaging requirements
- Services hub lists all 8 services (TMPL-04)
- Service detail template renders CMS content with JSON-LD (TMPL-15, SCHEMA-04)
- Locations hub shows province-grouped cities (TMPL-05)
- Breadcrumbs on all pages (SCHEMA-06)
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-build-templates-and-top-cities/02-04-SUMMARY.md`
</output>
