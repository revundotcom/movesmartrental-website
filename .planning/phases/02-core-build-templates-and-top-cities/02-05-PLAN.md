---
phase: 02-core-build-templates-and-top-cities
plan: 05
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/app/(site)/tenants/page.tsx
  - src/app/(site)/ca/[province]/[city]/[propertyType]/page.tsx
  - src/app/(site)/ca/[province]/[city]/[propertyType]/[bedroom]/page.tsx
  - src/app/(site)/ca/[province]/[city]/rentals/[slug]/page.tsx
autonomous: true
requirements:
  - TMPL-03
  - TMPL-22
  - TMPL-23
  - TEN-01
  - TEN-02
  - TEN-03
  - TEN-05
  - SCHEMA-07

must_haves:
  truths:
    - "Tenant hub page links to city rental pages and property type categories"
    - "Property category pages show listings in crawlable server-rendered HTML (not behind JS)"
    - "Property detail page renders full listing info with RealEstateListing JSON-LD"
    - "Bedroom count pages filter listings by bedroom number"
    - "PropertyCardBlock renders as Server Component with all listing data in HTML source"
  artifacts:
    - path: "src/app/(site)/tenants/page.tsx"
      provides: "Tenant hub template"
      min_lines: 60
    - path: "src/app/(site)/ca/[province]/[city]/[propertyType]/page.tsx"
      provides: "Property category template with server-rendered listings"
      min_lines: 50
    - path: "src/app/(site)/ca/[province]/[city]/[propertyType]/[bedroom]/page.tsx"
      provides: "Bedroom count filtered template"
      min_lines: 40
    - path: "src/app/(site)/ca/[province]/[city]/rentals/[slug]/page.tsx"
      provides: "Property detail template with RealEstateListing JSON-LD"
      min_lines: 60
  key_links:
    - from: "src/app/(site)/tenants/page.tsx"
      to: "src/sanity/queries/homepage.ts"
      via: "sanityFetch for featured cities"
      pattern: "sanityFetch"
    - from: "src/app/(site)/ca/[province]/[city]/[propertyType]/page.tsx"
      to: "src/sanity/queries/property-category.ts"
      via: "sanityFetch for category listings"
      pattern: "PROPERTY_CATEGORY_QUERY"
    - from: "src/app/(site)/ca/[province]/[city]/rentals/[slug]/page.tsx"
      to: "src/lib/schema-builders/real-estate-listing.ts"
      via: "buildRealEstateListingSchema"
      pattern: "buildRealEstateListingSchema"
---

<objective>
Build the tenant-facing page templates: Tenants hub, property category pages, bedroom count pages, and property detail pages.

Purpose: Tenant pages generate organic traffic through "apartments for rent in [city]" searches. Property listings MUST render as server-side HTML (not behind JavaScript) per TEN-05. The property detail page needs RealEstateListing JSON-LD for search engine rich results.

Output: 4 fully functional tenant-facing templates with CMS data, crawlable HTML listings, and structured data.
</objective>

<execution_context>
@/Users/sakshamsolanki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sakshamsolanki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-build-templates-and-top-cities/02-RESEARCH.md
@.planning/phases/02-core-build-templates-and-top-cities/02-01-SUMMARY.md

<interfaces>
From src/sanity/queries/property-category.ts (created in Plan 01):
```typescript
export const PROPERTY_CATEGORY_QUERY      // category + listings by city + type
export const PROPERTY_CATEGORY_PARAMS_QUERY // for generateStaticParams
export const BEDROOM_COUNT_QUERY          // filtered by bedroom count
```

From src/sanity/queries/property-listing.ts (created in Plan 01):
```typescript
export const PROPERTY_LISTING_QUERY        // single listing detail
export const PROPERTY_LISTING_PARAMS_QUERY // for generateStaticParams
```

From src/types/blocks.ts:
```typescript
export interface PropertyCardData {
  title: string; slug: string; citySlug: string; provinceSlug: string;
  price: number; bedrooms: number; bathrooms: number; sqft?: number;
  address: string; imageUrl?: string; imageAlt?: string; available: boolean
}
export interface PropertyCardBlockProps { listings: PropertyCardData[] }
export interface CityCardData { title, slug, provinceSlug, population?, medianRent? }
export interface CityGridBlockProps { cities: CityCardData[], province?, columns? }
```

From src/lib/schema-builders/index.ts:
```typescript
export { buildRealEstateListingSchema } from './real-estate-listing'
```

From src/components/layout/breadcrumb-nav.tsx:
```typescript
interface BreadcrumbCrumb { label: string; href: string }
export function BreadcrumbNav({ crumbs }: BreadcrumbNavProps)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Tenants hub and Property category templates</name>
  <files>
    src/app/(site)/tenants/page.tsx,
    src/app/(site)/ca/[province]/[city]/[propertyType]/page.tsx,
    src/app/(site)/ca/[province]/[city]/[propertyType]/[bedroom]/page.tsx
  </files>
  <action>
    **Tenants hub (src/app/(site)/tenants/page.tsx):**

    Replace skeleton. Server Component. TEN-01 requires: tenant hub linking to city rental pages and property type categories.

    generateMetadata: title "Tenants | Find Rentals | MoveSmart Rentals", description: "Browse verified rental listings across Ontario. Apartments, condos, houses, and townhouses available in 20+ cities."

    Data fetching: Fetch featured Tier-1 cities via sanityFetch. Tags: ['city'].

    Page sections:
    1. HeroBlock -- headline: "Find Your Next Rental Home", subheadline: "Browse verified listings in 20+ Ontario cities", cta1: { label: 'Browse Cities', href: '#cities' }, cta2: { label: 'How It Works', href: '#how-it-works' }
    2. Property type links section -- grid of 4 cards linking to general property type info:
       - "Apartments for Rent" -> /tenants/#apartments (informational links)
       - "Condos for Rent" -> /tenants/#condos
       - "Houses for Rent" -> /tenants/#houses
       - "Townhouses for Rent" -> /tenants/#townhouses
       Each card with icon (Building2, Building, Home, Castle from lucide-react), title, brief description
    3. CityGridBlock (id="cities") with featured cities, columns=4. Each city card links to /ca/{province}/{city}/
    4. HowItWorksBlock (id="how-it-works") with tenant journey steps:
       - Step 1: "Browse Listings" / "Search verified rentals by city, property type, and price range"
       - Step 2: "Apply Online" / "Submit your application with employment, credit, and reference details"
       - Step 3: "Get Approved" / "Our screening process gives you a decision within 48 hours"
       - Step 4: "Move In" / "Sign your lease and receive your keys on move-in day"
    5. FAQBlock with 4 tenant-specific FAQs (application process, required documents, lease terms, deposit)
    6. CTABannerBlock -- "Ready to Find Your New Home?", cta: "Browse Listings" -> /locations/

    **Property category template (src/app/(site)/ca/[province]/[city]/[propertyType]/page.tsx):**

    Replace the Plan 02 skeleton with full template. Server Component.

    generateStaticParams: Use PROPERTY_CATEGORY_PARAMS_QUERY. dynamicParams = true.

    generateMetadata: Fetch category SEO data, use generatePageMetadata with path `/ca/${province}/${city}/${propertyType}`, fallbackTitle: `${propertyType} for Rent in ${cityTitle}`, fallbackDescription about available listings.

    Page component:
    1. Fetch data with PROPERTY_CATEGORY_QUERY using params { citySlug, propertyType }. Tags: ['propertyCategory', 'propertyListing'].
    2. If no data, notFound()
    3. BreadcrumbNav: Home -> {province} -> {city} -> {propertyType title}
    4. HeroBlock: "{propertyType} for Rent in {cityTitle}" headline
    5. PortableTextBody: category description from CMS (if exists)
    6. Bedroom filter links: if category has listings, show links to bedroom count sub-pages:
       - "1 Bedroom" -> /ca/{province}/{city}/{propertyType}/1-bedroom/
       - "2 Bedroom" -> /ca/{province}/{city}/{propertyType}/2-bedroom/
       - "3+ Bedroom" -> /ca/{province}/{city}/{propertyType}/3-bedroom/
    7. PropertyCardBlock: render ALL listings from GROQ query as server-side HTML. CRITICAL: This must be a Server Component rendering. Do NOT use 'use client'. The listings must be in the HTML source for crawlers (TEN-05).
    8. CTABannerBlock with "Find Your Next Rental" messaging

    **Bedroom count template (src/app/(site)/ca/[province]/[city]/[propertyType]/[bedroom]/page.tsx):**

    Replace the Plan 02 skeleton. Server Component.

    No generateStaticParams (on-demand only). dynamicParams = true.

    generateMetadata: title "{bedroom} {propertyType} in {city}", description about filtered listings.

    Page component:
    1. Parse bedroom param: extract number from slug (e.g., "1-bedroom" -> 1, "2-bedroom" -> 2, "3-bedroom" -> 3)
    2. Fetch data with BEDROOM_COUNT_QUERY using params { citySlug, propertyType, bedrooms }. Tags: ['propertyCategory', 'propertyListing'].
    3. If no data, notFound()
    4. BreadcrumbNav: Home -> {province} -> {city} -> {propertyType} -> {bedroom}
    5. HeroBlock: "{bedroom} {propertyType} in {city}" headline
    6. PropertyCardBlock: filtered listings, server-rendered HTML
    7. Link back to full category page
    8. CTABannerBlock
  </action>
  <verify>
    <automated>cd /tmp/movesmart-project 2>/dev/null || cd "/Users/sakshamsolanki/Desktop/02-Client-Work/Canadian real estate" && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Tenants hub links to cities and property types; property category pages render server-side HTML listings; bedroom count pages filter by bedroom number; all compile cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Build Property detail template with RealEstateListing JSON-LD</name>
  <files>src/app/(site)/ca/[province]/[city]/rentals/[slug]/page.tsx</files>
  <action>
    Replace the Plan 02 skeleton with full property detail template. Server Component.

    generateStaticParams: Use PROPERTY_LISTING_PARAMS_QUERY. dynamicParams = true.

    generateMetadata: Fetch listing SEO data, use generatePageMetadata with path `/ca/${province}/${city}/rentals/${slug}`, fallbackTitle: `${listing.title} | ${cityTitle}`, fallbackDescription: `${listing.bedrooms} bedroom ${listing.propertyType} for rent at $${listing.price}/month in ${cityTitle}`.

    Page component:
    1. Fetch data with PROPERTY_LISTING_QUERY. Tags: ['propertyListing'].
    2. If no data, notFound()
    3. Build JSON-LD: buildRealEstateListingSchema with:
       - name: listing.title
       - description: excerpt from listing description
       - url: full page URL
       - price: listing.price
       - address: { streetAddress: listing.address, city: cityTitle, province: provinceTitle, country: 'CA' }
       - numberOfRooms: listing.bedrooms
    4. Inject via JsonLd component

    Page sections:
    1. BreadcrumbNav: Home -> {province} -> {city} -> Rentals -> {listing.title}
    2. Image gallery section (if listing.images): grid of next/image components, first image priority=true for LCP
    3. Listing details header:
       - h1: listing.title
       - Price: $X,XXX/month (formatted)
       - Address
       - Key stats row: bedrooms, bathrooms, sqft (if available)
       - Availability badge: "Available" or "Available from {date}"
    4. PortableTextBody: listing.description (if exists)
    5. Property details grid:
       - Bedrooms, Bathrooms, Square footage
       - Property type (from category reference)
       - Available date
    6. CTABannerBlock: "Interested in this property?", cta: "Apply Now" -> /contact/, secondary: "Book a Viewing" -> /contact/

    Ensure all listing content is in server-rendered HTML (TEN-05 requirement). No 'use client' directive anywhere in this file.
  </action>
  <verify>
    <automated>cd /tmp/movesmart-project 2>/dev/null || cd "/Users/sakshamsolanki/Desktop/02-Client-Work/Canadian real estate" && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Property detail renders full listing info, RealEstateListing JSON-LD present in source, images displayed, all content in server-rendered HTML, notFound() guard active</done>
</task>

</tasks>

<verification>
- Tenant hub has links to cities and property type categories
- Property category page: view source shows listing HTML (not JS-hidden)
- Property detail has RealEstateListing JSON-LD
- Bedroom count pages filter correctly
- BreadcrumbNav on all tenant pages
- No 'use client' directive in any listing template file
</verification>

<success_criteria>
- TEN-01: Tenant hub links to city rental and category pages
- TEN-02: Property category pages exist per city per type
- TEN-03: Bedroom count sub-pages exist
- TEN-05: Listings render in crawlable server HTML
- SCHEMA-07: RealEstateListing JSON-LD on property detail
- All pages have notFound() guards
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-build-templates-and-top-cities/02-05-SUMMARY.md`
</output>
