---
phase: 02-core-build-templates-and-top-cities
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(site)/faq/page.tsx
  - src/app/(site)/ca/[province]/[city]/[propertyType]/page.tsx
  - src/app/(site)/ca/[province]/[city]/[propertyType]/[bedroom]/page.tsx
  - src/app/(site)/ca/[province]/[city]/rentals/[slug]/page.tsx
autonomous: true
requirements:
  - SEO-08

must_haves:
  truths:
    - "Navigating to /faq/ returns a page (not 404)"
    - "Navigating to /ca/ontario/toronto/apartments-for-rent/ returns a page (not 404)"
    - "Navigating to /ca/ontario/toronto/apartments-for-rent/1-bedroom/ returns a page (not 404)"
    - "Navigating to /ca/ontario/toronto/rentals/sample-listing/ returns a page (not 404)"
    - "The 404 page shows helpful navigation links to homepage, owners, and tenants"
  artifacts:
    - path: "src/app/(site)/faq/page.tsx"
      provides: "FAQ hub route placeholder"
    - path: "src/app/(site)/ca/[province]/[city]/[propertyType]/page.tsx"
      provides: "Property category route"
    - path: "src/app/(site)/ca/[province]/[city]/[propertyType]/[bedroom]/page.tsx"
      provides: "Bedroom count route"
    - path: "src/app/(site)/ca/[province]/[city]/rentals/[slug]/page.tsx"
      provides: "Property detail route"
    - path: "src/app/not-found.tsx"
      provides: "Enhanced 404 page with navigation links"
  key_links:
    - from: "src/app/(site)/ca/[province]/[city]/[propertyType]/page.tsx"
      to: "Next.js file-system routing"
      via: "dynamic route segments"
      pattern: "\\[propertyType\\]"
---

<objective>
Create new route files needed for Phase 2 that do not yet exist in the file system, and enhance the 404 page with helpful navigation.

Purpose: Phase 1 created route skeletons for city, service, and city-service pages but NOT for property category, bedroom count, property detail, or FAQ routes. These file-system entries must exist before template plans can populate them. The 404 page enhancement ensures users who hit missing content get useful navigation.

Output: 4 new route files with minimal placeholder content + enhanced not-found.tsx.
</objective>

<execution_context>
@/Users/sakshamsolanki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sakshamsolanki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing route pattern from Phase 1 -->

From src/app/(site)/ca/[province]/[city]/[service]/page.tsx (skeleton pattern):
```typescript
export default async function CityServicePage({
  params,
}: {
  params: Promise<{ province: string; city: string; service: string }>
}) {
  const { province, city, service } = await params
  return <main>...</main>
}
```

From src/app/not-found.tsx (current minimal version):
```typescript
export default function NotFound() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center p-8">
      <h1 className="text-4xl font-bold">404 — Page Not Found</h1>
      <p className="mt-4 text-lg text-muted-foreground">...</p>
    </main>
  )
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 4 new route files for property and FAQ pages</name>
  <files>
    src/app/(site)/faq/page.tsx,
    src/app/(site)/ca/[province]/[city]/[propertyType]/page.tsx,
    src/app/(site)/ca/[province]/[city]/[propertyType]/[bedroom]/page.tsx,
    src/app/(site)/ca/[province]/[city]/rentals/[slug]/page.tsx
  </files>
  <action>
    Create 4 new route files following the Phase 1 skeleton pattern (async params with Promise type, await params, minimal placeholder content). These are SKELETON pages -- full template content comes in later plans.

    **src/app/(site)/faq/page.tsx:**
    - Static route (no dynamic params)
    - Export generateMetadata returning title "FAQ | MoveSmart Rentals"
    - Render placeholder: h1 "Frequently Asked Questions", p "Common questions about our services"

    **src/app/(site)/ca/[province]/[city]/[propertyType]/page.tsx:**
    - Dynamic params: `{ province: string; city: string; propertyType: string }`
    - Use async params pattern: `params: Promise<{ province: string; city: string; propertyType: string }>`
    - Export `export const dynamicParams = true`
    - Render placeholder with params displayed

    **src/app/(site)/ca/[province]/[city]/[propertyType]/[bedroom]/page.tsx:**
    - Dynamic params: `{ province: string; city: string; propertyType: string; bedroom: string }`
    - Use async params pattern with Promise
    - Export `export const dynamicParams = true`
    - Render placeholder with params displayed

    **src/app/(site)/ca/[province]/[city]/rentals/[slug]/page.tsx:**
    - Dynamic params: `{ province: string; city: string; slug: string }`
    - Use async params pattern with Promise
    - Export `export const dynamicParams = true`
    - Render placeholder with params displayed

    All pages must use the Next.js 15 async params pattern: `params: Promise<{...}>` with `await params`.
  </action>
  <verify>
    <automated>cd /tmp/movesmart-project 2>/dev/null || cd "/Users/sakshamsolanki/Desktop/02-Client-Work/Canadian real estate" && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>All 4 new route files exist, TypeScript compiles, routes are accessible in dev mode without 404</done>
</task>

<task type="auto">
  <name>Task 2: Enhance 404 page with helpful navigation links</name>
  <files>src/app/not-found.tsx</files>
  <action>
    Replace the current minimal not-found.tsx with an enhanced version per the research document pattern (SEO-08). The enhanced 404 page should:

    1. Import Link from 'next/link'
    2. Render a centered layout with max-w-4xl container
    3. H1: "Page Not Found" (text-4xl font-bold)
    4. Description paragraph: "The page you are looking for may have been moved or no longer exists."
    5. A grid of 6 navigation cards (sm:grid-cols-2 md:grid-cols-3) linking to:
       - Homepage (/) -- "Start from the beginning"
       - Property Owners (/owners/) -- "Management services"
       - Tenants (/tenants/) -- "Find your next rental"
       - Services (/services/) -- "Browse our services" (Note: this is a static route linking to service pages)
       - Locations (/locations/) -- "Find your city"
       - Contact (/contact/) -- "Get in touch"
    6. Each card: rounded-lg border p-6 hover:bg-gray-50 transition, with h2 (font-semibold) and p (text-sm text-muted-foreground)
    7. Export metadata: title "Page Not Found | MoveSmart Rentals"

    This prevents dead-end 404s and keeps users navigating, which is critical for SEO (reduces bounce rate from broken links).
  </action>
  <verify>
    <automated>cd /tmp/movesmart-project 2>/dev/null || cd "/Users/sakshamsolanki/Desktop/02-Client-Work/Canadian real estate" && npx tsc --noEmit --pretty 2>&1 | head -10</automated>
  </verify>
  <done>not-found.tsx renders 6 navigation cards with links to key sections, prevents dead-end 404 experience</done>
</task>

</tasks>

<verification>
- All 4 new route files exist and compile
- not-found.tsx has 6 navigation links
- `npm run build` succeeds with the new routes included
</verification>

<success_criteria>
- 4 new Next.js route files in the correct file-system locations
- Enhanced 404 page with 6 helpful navigation links
- TypeScript compiles without errors
- No existing routes broken by new route additions
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-build-templates-and-top-cities/02-02-SUMMARY.md`
</output>
