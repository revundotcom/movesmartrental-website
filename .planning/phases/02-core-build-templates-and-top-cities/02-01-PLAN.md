---
phase: 02-core-build-templates-and-top-cities
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sanity/queries/homepage.ts
  - src/sanity/queries/province.ts
  - src/sanity/queries/property-category.ts
  - src/sanity/queries/property-listing.ts
  - src/sanity/queries/blog-guide.ts
  - src/sanity/queries/comparison.ts
  - src/sanity/queries/case-study.ts
  - src/sanity/queries/faq.ts
  - src/components/portable-text.tsx
autonomous: true
requirements:
  - TMPL-16
  - SCHEMA-06

must_haves:
  truths:
    - "Every page template type has a GROQ query that fetches its data from Sanity"
    - "Portable Text content from CMS renders correctly with links, images, and lists"
    - "GROQ queries resolve references (city->province, service->slug) for URL construction"
  artifacts:
    - path: "src/sanity/queries/homepage.ts"
      provides: "Homepage data aggregation query (featured services, featured cities, stats)"
      exports: ["HOMEPAGE_QUERY"]
    - path: "src/sanity/queries/province.ts"
      provides: "Province page data + city list query"
      exports: ["PROVINCE_PAGE_QUERY", "PROVINCE_LIST_QUERY", "PROVINCE_CITIES_QUERY"]
    - path: "src/sanity/queries/property-category.ts"
      provides: "Property category page data by city + type"
      exports: ["PROPERTY_CATEGORY_QUERY", "PROPERTY_CATEGORY_PARAMS_QUERY", "BEDROOM_COUNT_QUERY"]
    - path: "src/sanity/queries/property-listing.ts"
      provides: "Individual listing data + listings by category"
      exports: ["PROPERTY_LISTING_QUERY", "PROPERTY_LISTINGS_BY_CATEGORY_QUERY", "PROPERTY_LISTING_PARAMS_QUERY"]
    - path: "src/sanity/queries/blog-guide.ts"
      provides: "Blog/guide page data + list for resources hub"
      exports: ["BLOG_GUIDE_QUERY", "BLOG_GUIDE_LIST_QUERY"]
    - path: "src/sanity/queries/comparison.ts"
      provides: "Comparison page data"
      exports: ["COMPARISON_QUERY", "COMPARISON_LIST_QUERY"]
    - path: "src/sanity/queries/case-study.ts"
      provides: "Case study page data"
      exports: ["CASE_STUDY_QUERY", "CASE_STUDY_LIST_QUERY"]
    - path: "src/sanity/queries/faq.ts"
      provides: "Aggregated FAQ from multiple document types"
      exports: ["AGGREGATED_FAQ_QUERY"]
    - path: "src/components/portable-text.tsx"
      provides: "Shared Portable Text renderer with custom components"
      exports: ["PortableTextBody", "portableTextComponents"]
  key_links:
    - from: "src/sanity/queries/*.ts"
      to: "next-sanity groq"
      via: "groq tagged template literal"
      pattern: "groq`"
    - from: "src/components/portable-text.tsx"
      to: "@portabletext/react"
      via: "import and component configuration"
      pattern: "PortableText"
---

<objective>
Create all GROQ queries needed by Phase 2 page templates and a shared Portable Text rendering component.

Purpose: Page templates cannot be built without data queries. This plan creates the data layer that all Wave 2 template plans consume. It also creates the shared Portable Text component used by every template that renders CMS body content.

Output: 8 GROQ query files covering homepage, province, property-category, property-listing, blog-guide, comparison, case-study, and FAQ aggregation. 1 Portable Text component with custom renderers for links, images, and lists.
</objective>

<execution_context>
@/Users/sakshamsolanki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sakshamsolanki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-build-templates-and-top-cities/02-RESEARCH.md

<interfaces>
<!-- Existing patterns to follow -->

From src/sanity/fetch.ts:
```typescript
export async function sanityFetch<T>({ query, params, tags }: {
  query: string; params?: Record<string, unknown>; tags?: string[]
}): Promise<T>
```

From src/sanity/queries/city-service.ts (example GROQ pattern):
```typescript
import { groq } from 'next-sanity'
export const CITY_SERVICE_PAGE_QUERY = groq`
  *[_type == "cityService" && city->slug.current == $citySlug ...][0] { ... }
`
```

From src/sanity/queries/city.ts:
```typescript
export const CITY_PAGE_QUERY = groq`*[_type == "city" && slug.current == $slug][0] { ... }`
export const CITY_LIST_QUERY = groq`*[_type == "city"] { slug, "provinceSlug": province->slug.current }`
```

From src/sanity/queries/service.ts:
```typescript
export const SERVICE_PAGE_QUERY = groq`*[_type == "service" && slug.current == $slug][0] { ... }`
export const SERVICE_LIST_QUERY = groq`*[_type == "service"] { slug }`
```

From src/types/sanity.ts (content types for query shapes):
- Province: title, slug, country, abbreviation, description, heroImage, seo, publishing
- City: title, slug, province (ref), tier, population, medianRent, vacancyRate, neighbourhoods, transitInfo, heroImage, description, seo
- Service: title, slug, shortDescription, icon, audience, order, heroImage, body, faq, seo
- CityService: city (ref), service (ref), localMedianRent, localVacancyRate, neighbourhoodNames, localRegulatory, localBody, heroHeadline, heroSubheadline, heroCta1, heroCta2, painPoints, benefits, howItWorks, testimonials, faq, seo
- BlogGuide: title, slug, category, city (ref), service (ref), body, excerpt, heroImage, author, seo
- Comparison: title, slug, service (ref), competitors, body, seo
- CaseStudy: title, slug, city (ref), clientName, outcome, body, heroImage, seo
- PropertyCategory: title, slug, city (ref), propertyType, description, averageRent, seo
- PropertyListing: title, slug, city (ref), category (ref), price, bedrooms, bathrooms, sqft, address, description, images, available, availableDate, seo

From src/types/blocks.ts:
- ServiceCardData: { title, slug, shortDescription, icon, audience }
- CityCardData: { title, slug, provinceSlug, population, medianRent, heroImageUrl, heroImageAlt }
- PropertyCardData: { title, slug, citySlug, provinceSlug, price, bedrooms, bathrooms, sqft, address, imageUrl, imageAlt, available }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all 8 GROQ query files for Phase 2 templates</name>
  <files>
    src/sanity/queries/homepage.ts,
    src/sanity/queries/province.ts,
    src/sanity/queries/property-category.ts,
    src/sanity/queries/property-listing.ts,
    src/sanity/queries/blog-guide.ts,
    src/sanity/queries/comparison.ts,
    src/sanity/queries/case-study.ts,
    src/sanity/queries/faq.ts
  </files>
  <action>
    Create 8 GROQ query files following the existing pattern in src/sanity/queries/city-service.ts (import groq from next-sanity, named exports, reference resolution with ->).

    **homepage.ts:**
    - HOMEPAGE_QUERY: Fetch featured services (`*[_type == "service"] | order(order asc)` projecting to ServiceCardData shape), featured Tier-1 cities (`*[_type == "city" && tier == 1]` projecting to CityCardData shape with `"provinceSlug": province->slug.current`), and a count of published city-service pages for stats.

    **province.ts:**
    - PROVINCE_PAGE_QUERY: Fetch province by slug with all fields. Include `"cities": *[_type == "city" && province._ref == ^._id] | order(title asc) { title, slug, tier, population, medianRent, "provinceSlug": ^.slug.current }`.
    - PROVINCE_LIST_QUERY: All provinces with slug for generateStaticParams: `*[_type == "province"] { slug, country }`.
    - PROVINCE_CITIES_QUERY: Cities by province slug for CityGridBlock: `*[_type == "city" && province->slug.current == $provinceSlug] | order(tier asc, title asc) { title, slug, tier, population, medianRent, "provinceSlug": province->slug.current }`.

    **property-category.ts:**
    - PROPERTY_CATEGORY_QUERY: Fetch property category by city slug + propertyType with listings: `*[_type == "propertyCategory" && city->slug.current == $citySlug && propertyType == $propertyType][0]` with resolved city->province references and nested listings `"listings": *[_type == "propertyListing" && category._ref == ^._id && available == true] | order(price asc)` projecting to PropertyCardData shape.
    - PROPERTY_CATEGORY_PARAMS_QUERY: All property categories with city+province slugs for generateStaticParams.
    - BEDROOM_COUNT_QUERY: Like PROPERTY_CATEGORY_QUERY but filtered by bedrooms parameter: listings where `bedrooms == $bedrooms`.

    **property-listing.ts:**
    - PROPERTY_LISTING_QUERY: Single listing by slug with resolved city/province/category references, all fields including images array, description body.
    - PROPERTY_LISTINGS_BY_CATEGORY_QUERY: Listings by category for PropertyCardBlock display.
    - PROPERTY_LISTING_PARAMS_QUERY: All listings with city+province slugs for generateStaticParams.

    **blog-guide.ts:**
    - BLOG_GUIDE_QUERY: Single blog/guide by slug with resolved city and service references, full body, author, dates.
    - BLOG_GUIDE_LIST_QUERY: All published blog/guides ordered by publishedAt, with slug, title, category, excerpt for resources hub listing. Filter by category if $category param provided.

    **comparison.ts:**
    - COMPARISON_QUERY: Single comparison by slug with resolved service reference, competitors array, body content.
    - COMPARISON_LIST_QUERY: All comparisons with slug, title for listing/linking.

    **case-study.ts:**
    - CASE_STUDY_QUERY: Single case study by slug with resolved city reference, outcome, body, heroImage.
    - CASE_STUDY_LIST_QUERY: All case studies with slug, title, clientName, outcome for listing.

    **faq.ts:**
    - AGGREGATED_FAQ_QUERY: Collect FAQ items from multiple document types using GROQ union: services FAQ, city-services FAQ. Return as flat array of {question, answer, source, sourceTitle}. This powers the FAQ hub page (TMPL-11).

    For all queries:
    - Always resolve references with -> syntax for slugs, titles, province data
    - Project only needed fields (no `*` wildcard projections)
    - Include seo and publishing fields on page-level queries
    - Tag correctly for ISR revalidation (tag = document _type name)
  </action>
  <verify>
    <automated>cd /tmp/movesmart-project 2>/dev/null || cd "/Users/sakshamsolanki/Desktop/02-Client-Work/Canadian real estate" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>All 8 query files exist in src/sanity/queries/, TypeScript compiles cleanly, every query uses groq tagged template literal and exports named constants</done>
</task>

<task type="auto">
  <name>Task 2: Create shared Portable Text rendering component</name>
  <files>src/components/portable-text.tsx</files>
  <action>
    Create a shared Portable Text component at src/components/portable-text.tsx that wraps @portabletext/react with custom component definitions. This component will be used by every template that renders CMS body content (blog, guide, case study, comparison, city-service localBody, service body, city description).

    The component should:
    1. Import PortableText from '@portabletext/react' and Image from 'next/image'
    2. Define a portableTextComponents config object with:
       - types.image: Renders SanityImage as next/image with width=800, height=400, className for responsive sizing, and alt text from value.alt
       - marks.link: Renders anchor tags with className="text-primary underline hover:text-primary/80", target="_blank" + rel="noopener noreferrer" for external links (starts with http), no target for internal links. Support allowRelative per Phase 1 decision.
       - marks.strong: Renders <strong> with className
       - marks.em: Renders <em>
       - block.h2: Renders <h2> with consistent className "text-2xl font-bold mt-8 mb-4"
       - block.h3: Renders <h3> with className "text-xl font-semibold mt-6 mb-3"
       - block.blockquote: Renders <blockquote> with border-left styling
       - list.bullet: Renders <ul> with list-disc styling
       - list.number: Renders <ol> with list-decimal styling
       - listItem: Renders <li> with proper spacing
    3. Export a PortableTextBody component that wraps PortableText with the custom components and a prose container div: `<div className="prose prose-lg max-w-none"><PortableText value={value} components={portableTextComponents} /></div>`
    4. Export portableTextComponents separately for cases where templates need to customize further

    This is a Server Component (no 'use client' directive). The PortableText component from @portabletext/react works in Server Components.

    NOTE: For image rendering, use a simple URL construction from the asset ref pattern: `https://cdn.sanity.io/images/${projectId}/${dataset}/${ref-part}`. Import the project ID and dataset from sanity client config. If @sanity/image-url is not installed, use this manual URL construction pattern.
  </action>
  <verify>
    <automated>cd /tmp/movesmart-project 2>/dev/null || cd "/Users/sakshamsolanki/Desktop/02-Client-Work/Canadian real estate" && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>src/components/portable-text.tsx exists, exports PortableTextBody and portableTextComponents, TypeScript compiles cleanly, component handles images, links, headings, lists, and blockquotes</done>
</task>

</tasks>

<verification>
- All 8 GROQ query files compile with TypeScript
- Portable Text component compiles and handles all block types
- Query projections match the existing TypeScript types in src/types/sanity.ts and src/types/blocks.ts
- No new npm dependencies needed (all already installed from Phase 1)
</verification>

<success_criteria>
- 8 new GROQ query files in src/sanity/queries/ with properly typed exports
- 1 Portable Text component with custom renderers for all content block types
- `npm run build` succeeds (or at minimum `npx tsc --noEmit` succeeds)
- All queries follow existing patterns (groq tagged literal, reference resolution, field projection)
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-build-templates-and-top-cities/02-01-SUMMARY.md`
</output>
