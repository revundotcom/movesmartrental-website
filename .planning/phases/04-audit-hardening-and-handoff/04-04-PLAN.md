---
phase: 04-audit-hardening-and-handoff
plan: 04
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - .planning/handoff/source-code-transfer.md
autonomous: true
requirements: [HAND-01]

must_haves:
  truths:
    - "All source code is committed to the repository with no uncommitted changes"
    - "A transfer checklist confirms the repo is in (or ready for) the REVUN GitHub account"
    - "A clone-and-deploy verification script or checklist exists"
  artifacts:
    - path: ".planning/handoff/source-code-transfer.md"
      provides: "Transfer checklist and clone-deploy verification steps"
      min_lines: 40
  key_links:
    - from: ".planning/handoff/source-code-transfer.md"
      to: ".planning/handoff/environment-documentation.md"
      via: "references deployment process"
      pattern: "environment-documentation"
---

<objective>
Verify all source code is committed, create a source code transfer checklist, and document the clone-and-deploy verification process for the REVUN GitHub account.

Purpose: HAND-01 requires the full source code to be in the client's REVUN GitHub account. This plan ensures the codebase is clean, all changes from CWV optimization (Plan 01) are committed, and provides a transfer/verification checklist.

Output: source-code-transfer.md with transfer checklist and clone-deploy verification steps.
</objective>

<execution_context>
@/Users/sakshamsolanki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sakshamsolanki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify codebase cleanliness and create source code transfer checklist</name>
  <files>.planning/handoff/source-code-transfer.md</files>
  <action>
    1. Run `git status` to verify no uncommitted changes exist. If there are uncommitted changes from prior plans, note them but do NOT commit them (that's the executor's job for each prior plan).

    2. Run `git log --oneline -20` to see recent commit history and confirm all Phase 4 work is committed.

    3. Verify .gitignore includes standard Next.js exclusions (node_modules, .next, .env.local, etc.) and does NOT accidentally exclude any source files.

    4. Create .planning/handoff/source-code-transfer.md with:

    **Source Code Transfer Checklist**

    Section 1: Pre-Transfer Verification
    - [ ] All source code committed (no uncommitted changes)
    - [ ] .gitignore properly configured (node_modules, .next, .env.local excluded)
    - [ ] .env.example is up to date with all required env vars
    - [ ] No secrets or credentials in committed files (verify .env.local is gitignored)
    - [ ] README.md or equivalent exists with setup instructions

    Section 2: Repository Transfer Steps
    - Option A (if repo is already in REVUN account): Verify client has admin access
    - Option B (if repo needs transfer): Steps to transfer GitHub repo ownership
    - Option C (if fresh push needed): Steps to push to REVUN GitHub account
      1. Client creates empty repo in REVUN GitHub org
      2. Add REVUN repo as remote: `git remote add revun https://github.com/REVUN/movesmart-rentals.git`
      3. Push all branches: `git push revun main`
      4. Verify all commits, branches, and tags transferred

    Section 3: Clone-and-Deploy Verification
    Step-by-step to verify a fresh clone produces a working site:
    1. Clone repo: `git clone <revun-repo-url>`
    2. Install dependencies: `npm install`
    3. Copy .env.example to .env.local: `cp .env.example .env.local`
    4. Fill in all env vars (reference environment-documentation.md)
    5. Run dev server: `npm run dev` — verify http://localhost:3000 loads
    6. Run production build: `npm run build` — verify no build errors
    7. Deploy to Vercel:
       a. Import repo in Vercel dashboard
       b. Set all env vars from .env.example
       c. Deploy — verify site loads at the assigned URL
    8. Configure Sanity webhook for ISR revalidation (reference environment-documentation.md)
    9. Publish a test document in Sanity Studio — verify page updates on site

    Section 4: Post-Transfer Verification
    - [ ] Client can access repo on GitHub
    - [ ] Client can clone and run locally
    - [ ] Client can deploy to Vercel
    - [ ] Sanity Studio works at /studio
    - [ ] ISR revalidation fires on publish
    - [ ] All pages render correctly on deployed site

    Section 5: Ongoing Maintenance Notes
    - How to update dependencies: `npm update` (minor), manual major version bumps
    - How to add new pages: Create document in Sanity, ISR handles the rest
    - When a developer IS needed: Next.js upgrades, new page template types, schema changes, new integrations
    - When a developer is NOT needed: New city pages, new blog posts, content updates, SEO field changes
  </action>
  <verify>
    <automated>cd "/Users/sakshamsolanki/Desktop/02-Client-Work/Canadian real estate" && wc -l .planning/handoff/source-code-transfer.md && grep -c "\- \[" .planning/handoff/source-code-transfer.md</automated>
  </verify>
  <done>source-code-transfer.md has pre-transfer verification, transfer steps, clone-and-deploy verification, and post-transfer checklist. At least 40 lines with multiple checklist items.</done>
</task>

</tasks>

<verification>
- source-code-transfer.md exists with all 5 sections
- Document references environment-documentation.md for env var details
- Clone-and-deploy steps are specific and numbered
- No actual credentials appear in the document
</verification>

<success_criteria>
1. Transfer checklist covers all steps from pre-verification to post-transfer
2. Clone-and-deploy steps are specific enough that a developer unfamiliar with the project can follow them
3. Document clearly distinguishes what needs a developer vs. what the content team can do
</success_criteria>

<output>
After completion, create `.planning/phases/04-audit-hardening-and-handoff/04-04-SUMMARY.md`
</output>
