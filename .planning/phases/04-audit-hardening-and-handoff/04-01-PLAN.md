---
phase: 04-audit-hardening-and-handoff
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - src/app/layout.tsx
  - src/components/blocks/hero-block.tsx
  - src/components/blocks/city-grid-block.tsx
  - src/components/blocks/property-card-block.tsx
  - src/components/portable-text.tsx
  - src/app/(site)/resources/[slug]/page.tsx
  - src/app/(site)/ca/[province]/[city]/rentals/[slug]/page.tsx
autonomous: true
requirements: [SEO-09, SEO-10]

must_haves:
  truths:
    - "LCP image on homepage loads with priority and correct sizes attribute"
    - "All non-LCP images use lazy loading (default next/image behavior)"
    - "next.config.ts enables WebP/AVIF format optimization"
    - "CLS is minimized by explicit width/height or fill+sizes on all Image components"
    - "No render-blocking client-side JS in the critical path"
  artifacts:
    - path: "next.config.ts"
      provides: "Image format and optimization config"
      contains: "formats"
    - path: "src/components/blocks/hero-block.tsx"
      provides: "LCP image with priority flag and sizes"
      contains: "priority"
    - path: "src/app/layout.tsx"
      provides: "Optimized font loading and script strategy"
      contains: "display"
  key_links:
    - from: "next.config.ts"
      to: "all Image components"
      via: "Next.js image optimization pipeline"
      pattern: "formats.*webp\\|avif"
    - from: "src/components/blocks/hero-block.tsx"
      to: "next/image"
      via: "priority prop for LCP"
      pattern: "priority.*true"
---

<objective>
Optimize Core Web Vitals (LCP < 2.5s, INP < 200ms, CLS < 0.1) and image delivery across the entire site.

Purpose: SEO-09 and SEO-10 are the final technical SEO requirements. CWV directly impacts Google ranking and user experience. Image optimization reduces bandwidth and speeds up page loads.

Output: Updated next.config.ts with image format config, all Image components with correct priority/sizes/loading attributes, font display optimization, and script loading improvements.
</objective>

<execution_context>
@/Users/sakshamsolanki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sakshamsolanki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@next.config.ts
@src/app/layout.tsx
@src/components/blocks/hero-block.tsx
@src/components/blocks/city-grid-block.tsx
@src/components/blocks/property-card-block.tsx
@src/components/portable-text.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/types/blocks.ts:
```typescript
export interface HeroBlockProps {
  headline: string
  subheadline?: string
  cta1?: { label: string; href: string; variant?: string }
  cta2?: { label: string; href: string; variant?: string }
  backgroundImageUrl?: string
  backgroundImageAlt?: string
  priority?: boolean  // true for above-fold images (LCP)
  city?: string
  service?: string
}
```

From next.config.ts (current):
```typescript
const nextConfig: NextConfig = {
  trailingSlash: true,
  images: {
    remotePatterns: [
      { protocol: 'https', hostname: 'cdn.sanity.io' },
    ],
  },
}
```

From src/app/layout.tsx (current):
- Inter font loaded via `next/font/google` (no `display` option set -- defaults to 'swap' but should be explicit)
- GoogleTagManager loaded unconditionally in body
- SalesIQ loaded with `strategy="lazyOnload"` (already good)
- ScrollDepthTracker and LinkTracker loaded eagerly (potential INP concern)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure image optimization and fix LCP/CLS across all Image components</name>
  <files>next.config.ts, src/components/blocks/hero-block.tsx, src/components/blocks/city-grid-block.tsx, src/components/blocks/property-card-block.tsx, src/components/portable-text.tsx, src/app/(site)/resources/[slug]/page.tsx, src/app/(site)/ca/[province]/[city]/rentals/[slug]/page.tsx</files>
  <action>
    1. Update next.config.ts to add image format optimization:
       - Add `formats: ['image/avif', 'image/webp']` inside `images` config (AVIF first for best compression, WebP fallback)
       - Add `deviceSizes: [640, 750, 828, 1080, 1200, 1920]` and `imageSizes: [16, 32, 48, 64, 96, 128, 256, 384]` for responsive srcset generation
       - These are Next.js defaults but making them explicit ensures they survive config changes

    2. Audit and fix every `<Image>` usage in the codebase:

       hero-block.tsx (LCP candidate -- above the fold):
       - Already has `priority={priority}` and `sizes="100vw"` -- GOOD, leave as-is
       - Confirm `fill` prop is used (prevents CLS) -- already present

       city-grid-block.tsx:
       - Add `sizes="(max-width: 768px) 100vw, (max-width: 1200px) 50vw, 33vw"` if missing
       - Ensure NO `priority` (these are below fold)
       - Add `className` with `object-cover` if not present
       - Add explicit `width` and `height` if using non-fill layout to prevent CLS

       property-card-block.tsx:
       - Add `sizes="(max-width: 768px) 100vw, (max-width: 1200px) 50vw, 33vw"` if missing
       - Ensure NO `priority` (below fold)
       - Verify width/height or fill is set to prevent CLS

       portable-text.tsx (inline content images):
       - These are always below fold -- ensure `loading="lazy"` is default (it is for next/image when priority is false)
       - Add `sizes="(max-width: 768px) 100vw, 672px"` (max content width) if rendering inline images
       - Ensure width/height are provided from Sanity asset metadata to prevent CLS

       resources/[slug]/page.tsx (blog featured images):
       - If hero/featured image, add `priority` only if it's the first visible image
       - Add appropriate `sizes` attribute
       - Ensure width/height from Sanity metadata

       rentals/[slug]/page.tsx (property detail images):
       - Main property image should have `priority` if it's above the fold
       - Gallery images should NOT have priority
       - Add appropriate `sizes`

    3. Do NOT add any new npm packages. next/image handles WebP/AVIF conversion server-side automatically.
  </action>
  <verify>
    <automated>cd "/Users/sakshamsolanki/Desktop/02-Client-Work/Canadian real estate" && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>next.config.ts has formats: ['image/avif', 'image/webp'], every Image component has explicit sizes attribute, LCP images have priority=true, non-LCP images have no priority, all images have width/height or fill to prevent CLS</done>
</task>

<task type="auto">
  <name>Task 2: Optimize font loading, script strategy, and INP for Core Web Vitals</name>
  <files>src/app/layout.tsx</files>
  <action>
    1. Font optimization in layout.tsx:
       - Add `display: 'swap'` to the Inter font config: `const inter = Inter({ subsets: ['latin'], display: 'swap' })` -- this is the default but being explicit prevents regressions and ensures FOUT over FOIT for LCP
       - Add `preload: true` (also default, but explicit)

    2. Script loading optimization in layout.tsx:
       - GoogleTagManager is already loaded via @next/third-parties which handles optimization internally -- leave as-is
       - SalesIQ already uses `strategy="lazyOnload"` -- GOOD, leave as-is
       - ScrollDepthTracker and LinkTracker are client components loaded in the root layout. Verify they use 'use client' and passive event listeners. If they attach scroll/click handlers, ensure they use `{ passive: true }` option to avoid blocking the main thread (INP impact)

    3. Add `fetchPriority="high"` consideration:
       - The Inter font preload link is handled by next/font automatically -- no manual intervention needed
       - Do NOT add preconnect to cdn.sanity.io -- Next.js Image component handles this automatically

    4. Verify no unnecessary re-renders:
       - Ensure ScrollDepthTracker and LinkTracker do NOT cause layout shifts (they should only listen, not render visible DOM)
       - If they wrap children or render visible elements, they could cause CLS -- verify and fix if needed

    5. Do NOT add any third-party CWV monitoring libraries. The site is verified via PageSpeed Insights manually.
  </action>
  <verify>
    <automated>cd "/Users/sakshamsolanki/Desktop/02-Client-Work/Canadian real estate" && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Inter font has explicit display:'swap', ScrollDepthTracker and LinkTracker use passive event listeners, no render-blocking scripts in critical path, build succeeds with no errors</done>
</task>

</tasks>

<verification>
- `next build` completes successfully with no errors
- next.config.ts contains `formats: ['image/avif', 'image/webp']`
- Every `<Image>` in the codebase has a `sizes` attribute (grep confirms)
- No `<Image>` component below the fold has `priority={true}`
- Inter font config includes `display: 'swap'`
</verification>

<success_criteria>
1. Build passes with image optimization config active
2. All Image components have explicit sizes for responsive loading
3. LCP-candidate images (hero blocks) use priority, non-LCP images do not
4. Font loading is optimized with display swap
5. No render-blocking scripts in the critical rendering path
</success_criteria>

<output>
After completion, create `.planning/phases/04-audit-hardening-and-handoff/04-01-SUMMARY.md`
</output>
