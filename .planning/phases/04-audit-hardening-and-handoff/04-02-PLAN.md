---
phase: 04-audit-hardening-and-handoff
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - .planning/handoff/environment-documentation.md
  - .planning/handoff/credentials-transfer.md
autonomous: true
requirements: [HAND-02, HAND-05]

must_haves:
  truths:
    - "A developer can read the environment doc and set up a working local dev environment from scratch"
    - "The client has a single document listing every credential, where it lives, and how to access it"
    - ".env.example lists every env var the project uses with descriptions"
  artifacts:
    - path: ".env.example"
      provides: "Complete env var reference with all vars"
      contains: "NEXT_PUBLIC_GTM_ID"
    - path: ".planning/handoff/environment-documentation.md"
      provides: "Architecture overview, env vars, deployment process, ISR strategy"
      min_lines: 100
    - path: ".planning/handoff/credentials-transfer.md"
      provides: "All credentials with access instructions"
      min_lines: 60
  key_links:
    - from: ".env.example"
      to: ".planning/handoff/environment-documentation.md"
      via: "env var descriptions cross-reference"
      pattern: "NEXT_PUBLIC"
---

<objective>
Create complete environment documentation and credentials transfer document for client handoff.

Purpose: HAND-02 requires the client to understand the architecture, env vars, and deployment process. HAND-05 requires all credentials to be listed in one place for independent operation. These are critical for the client to operate without contractor involvement after handoff.

Output: Updated .env.example with all vars, environment-documentation.md covering architecture and deployment, credentials-transfer.md listing every service credential.
</objective>

<execution_context>
@/Users/sakshamsolanki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sakshamsolanki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.env.example
@src/app/layout.tsx
@next.config.ts
@package.json

<interfaces>
<!-- All env vars used in the project (extracted from codebase grep) -->

Public (client-side) env vars:
- NEXT_PUBLIC_SANITY_PROJECT_ID — Sanity project ID
- NEXT_PUBLIC_SANITY_DATASET — Sanity dataset name (usually "production")
- NEXT_PUBLIC_SITE_URL — Canonical site URL (https://movesmartrentals.com)
- NEXT_PUBLIC_GTM_ID — Google Tag Manager container ID
- NEXT_PUBLIC_GSC_VERIFICATION — Google Search Console verification meta tag
- NEXT_PUBLIC_BING_VERIFICATION — Bing Webmaster Tools verification meta tag
- NEXT_PUBLIC_RECAPTCHA_SITE_KEY — reCAPTCHA v3 site key
- NEXT_PUBLIC_SALESIQ_WIDGET_CODE — Zoho SalesIQ widget code

Server-side env vars:
- SANITY_API_READ_TOKEN — Sanity API read token for server-side data fetching
- SANITY_WEBHOOK_SECRET — Sanity webhook secret for ISR revalidation
- RECAPTCHA_SECRET_KEY — reCAPTCHA v3 secret key for server-side verification

Services in use:
- Vercel (hosting, deployment, edge CDN)
- Sanity (CMS, Studio)
- Google Tag Manager (GTM)
- Google Analytics 4 (GA4, via GTM)
- Google Search Console (GSC)
- Bing Webmaster Tools
- Zoho SalesIQ (chat widget)
- Google reCAPTCHA v3 (form spam protection)
- GitHub (REVUN account, source code)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update .env.example and create environment documentation</name>
  <files>.env.example, .planning/handoff/environment-documentation.md</files>
  <action>
    1. Update .env.example to include ALL env vars used in the project (the current file only has 5 of 11 vars). Add each var with a comment explaining its purpose and where to get the value:
       ```
       # Sanity CMS
       NEXT_PUBLIC_SANITY_PROJECT_ID=        # Sanity Dashboard > Project > Settings > API
       NEXT_PUBLIC_SANITY_DATASET=production  # Usually "production"
       SANITY_API_READ_TOKEN=                 # Sanity Dashboard > API > Tokens > Add token (Viewer)
       SANITY_WEBHOOK_SECRET=                 # Random string, must match Sanity webhook config

       # Site
       NEXT_PUBLIC_SITE_URL=https://movesmartrentals.com

       # Google Tag Manager
       NEXT_PUBLIC_GTM_ID=                    # GTM Dashboard > Container ID (GTM-XXXXXXX)

       # Google Search Console
       NEXT_PUBLIC_GSC_VERIFICATION=          # GSC > Settings > Ownership verification > HTML tag

       # Bing Webmaster Tools
       NEXT_PUBLIC_BING_VERIFICATION=         # Bing Webmaster > Settings > Ownership verification

       # reCAPTCHA v3
       NEXT_PUBLIC_RECAPTCHA_SITE_KEY=        # Google reCAPTCHA admin > Site key
       RECAPTCHA_SECRET_KEY=                  # Google reCAPTCHA admin > Secret key

       # Zoho SalesIQ
       NEXT_PUBLIC_SALESIQ_WIDGET_CODE=       # SalesIQ Dashboard > Settings > Widget code
       ```

    2. Create .planning/handoff/environment-documentation.md covering:

       **Architecture Overview:**
       - Next.js 15 App Router with TypeScript and Tailwind v4
       - Sanity CMS v3 (Free Tier) with 9 content types and 10 block components
       - Vercel deployment with automatic SSL, edge CDN
       - ISR (Incremental Static Regeneration) via Sanity webhook-triggered revalidation
       - URL structure: /ca/{province}/{city}/{service}/ and /us/{state}/{city}/{service}/

       **Directory Structure:**
       - src/app/ — Next.js pages and API routes (App Router)
       - src/app/(site)/ — Public-facing pages under shared layout
       - src/app/api/ — API routes (contact form, revalidation, analytics)
       - src/app/studio/ — Sanity Studio (embedded at /studio)
       - src/components/ — React components (blocks/, layout/, tracking/, ui/)
       - src/sanity/ — Sanity client, fetch helper, schemas, queries
       - src/lib/ — Utility functions, metadata helpers, schema builders
       - src/types/ — TypeScript type definitions

       **Environment Variables:** Reference .env.example for complete list. Explain each var's purpose and where to obtain the value. Note which are public vs server-side.

       **Local Development:**
       - Prerequisites: Node.js 18+, npm
       - Steps: clone repo, copy .env.example to .env.local, fill in values, npm install, npm run dev
       - Dev server runs at http://localhost:3000
       - Sanity Studio at http://localhost:3000/studio

       **Deployment:**
       - Push to main branch triggers Vercel auto-deploy
       - Environment variables must be set in Vercel Dashboard > Settings > Environment Variables
       - All vars from .env.example must be added to Vercel
       - Preview deployments created for pull requests automatically

       **ISR Revalidation:**
       - How it works: Sanity webhook POST to /api/revalidate with secret
       - Sanity Dashboard > API > Webhooks > configured to fire on publish/unpublish
       - Pages revalidate within seconds of CMS changes (no full rebuild needed)

       **Content Publishing Flow:**
       - Editor logs into Sanity Studio at /studio
       - Creates/edits document (City, CityService, BlogGuide, etc.)
       - Clicks Publish
       - Webhook fires automatically, page updates on site within 60 seconds

       **Key Dependencies:** List the major npm packages and their purposes (next, sanity, next-sanity, @next/third-parties, @portabletext/react, lucide-react, zod, react-hook-form)
  </action>
  <verify>
    <automated>cd "/Users/sakshamsolanki/Desktop/02-Client-Work/Canadian real estate" && grep -c "NEXT_PUBLIC\|SANITY_\|RECAPTCHA" .env.example && wc -l .planning/handoff/environment-documentation.md</automated>
  </verify>
  <done>.env.example lists all 11 env vars with comments. environment-documentation.md covers architecture, directory structure, env vars, local dev setup, deployment, ISR, and content publishing flow in at least 100 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Create credentials transfer document</name>
  <files>.planning/handoff/credentials-transfer.md</files>
  <action>
    Create .planning/handoff/credentials-transfer.md as a structured handoff checklist. This document lists every service credential the client needs to operate independently. Use a checklist format so the contractor and client can track what has been transferred.

    Structure:

    **Header:** Title, date, from/to, purpose statement

    **For each service, document:**
    - Service name and URL
    - What it's used for in the project
    - Account owner / email
    - How to access (dashboard URL)
    - Credentials to transfer (login, API keys, tokens)
    - Transfer checklist item

    **Services to document (9 services):**

    1. **GitHub (REVUN account)** — Source code repository
       - Dashboard: github.com
       - Credential: Repository access (owner account)
       - Action: Verify client has admin access to the repo

    2. **Vercel** — Hosting and deployment
       - Dashboard: vercel.com/dashboard
       - Credentials: Account login, project access
       - Action: Transfer project ownership or ensure client team is added

    3. **Sanity CMS** — Content management
       - Dashboard: sanity.io/manage
       - Credentials: Project access, API tokens, Studio login
       - Action: Add client as project admin, share API token locations

    4. **Google Tag Manager** — Tag management
       - Dashboard: tagmanager.google.com
       - Credentials: Container access
       - Action: Add client Google account as admin to GTM container

    5. **Google Analytics 4** — Traffic analytics
       - Dashboard: analytics.google.com
       - Credentials: Property access
       - Action: Add client as admin to GA4 property

    6. **Google Search Console** — Search performance
       - Dashboard: search.google.com/search-console
       - Credentials: Property verification
       - Action: Add client as owner (not just user) of the property

    7. **Bing Webmaster Tools** — Bing search presence
       - Dashboard: bing.com/webmasters
       - Credentials: Site verification
       - Action: Add client account as admin

    8. **Google reCAPTCHA** — Form spam protection
       - Dashboard: google.com/recaptcha/admin
       - Credentials: Site key + secret key
       - Action: Transfer reCAPTCHA site ownership or share keys

    9. **Zoho SalesIQ** — Live chat widget
       - Dashboard: salesiq.zoho.com
       - Credentials: Account login, widget code
       - Action: Ensure client has admin access to SalesIQ portal

    **Domain registrar note:** The domain (movesmartrentals.com) is assumed to be already under client control. Document DNS settings pointing to Vercel if applicable.

    **Final checklist section:** A summary checkbox list of all 9 services with transfer status.

    **Important:** Do NOT include actual credential values in this document. Only document where to find them and what needs to be transferred.
  </action>
  <verify>
    <automated>cd "/Users/sakshamsolanki/Desktop/02-Client-Work/Canadian real estate" && wc -l .planning/handoff/credentials-transfer.md && grep -c "\- \[" .planning/handoff/credentials-transfer.md</automated>
  </verify>
  <done>credentials-transfer.md lists all 9 services with access instructions and transfer checklists, at least 60 lines, contains checklist items for tracking transfer status</done>
</task>

</tasks>

<verification>
- .env.example contains all 11 env vars used in the codebase
- environment-documentation.md is at least 100 lines and covers architecture, env vars, deployment, ISR
- credentials-transfer.md lists all 9 services with transfer checklists
- No actual secrets/credentials appear in any document
</verification>

<success_criteria>
1. A new developer can set up a local dev environment using only the environment documentation
2. The client has a single checklist to track credential transfer for all 9 services
3. .env.example is complete and matches actual usage in the codebase
</success_criteria>

<output>
After completion, create `.planning/phases/04-audit-hardening-and-handoff/04-02-SUMMARY.md`
</output>
